import React from 'react';

const containerDiv = {
  padding: '25px',
};

const NotFoundPage = () => (
  <React.Fragment>
    <h1 style={containerDiv}>404 Not Found</h1>\
  </React.Fragment>
);

export default NotFoundPage;
